# A05 Carousel Site

Live site: https://bnhvya.github.io/A05/

## Project files
- `index.html` - page structure
- `styles.css` - carousel/music-box styling
- `script.js` - 50-image carousel logic, text phrases, looping audio
- `animals/` - image assets
- `sounds/` - audio asset

## Run locally
Open `index.html` in your browser.

## Update and republish
From `c:\Users\jessi\Downloads\A05`:

```powershell
git add .
git commit -m "Describe your change"
git push
```

GitHub Pages will auto-deploy from `main`.
